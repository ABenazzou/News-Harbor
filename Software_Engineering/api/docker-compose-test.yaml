version: '3'
services:
  news_harbor_api:
    build: .
    container_name: "news-harbor-api-test-container"
    ports:
      - "15800:15800"
    volumes:
      - /home/abenazzou/conf/:/config # mapping where pem files exist
